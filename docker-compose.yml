version: '2'

services:
  web:
    build: .
    command: ./bin/docker/ruby_entrypoint bundle exec foreman start -f ./Procfile.dev -p 3000
    volumes:
      - .:/app
      - rubygems_cache:/rubygems
    ports:
      - '3000:3000'
    environment:
      GEM_HOME: '/rubygems'
      BUNDLE_PATH: '/rubygems'
      DATABASE_URL: 'postgres://postgres:@postgres:5432'
      REDIS_URL: 'redis://redis:6379'
    depends_on:
      - postgres
      - redis
    # needed for Codeship Pro:
    links:
      - postgres
      - redis

  yarn:
    build: .
    command: ./bin/docker/yarn_entrypoint yarn run webpack --watch
    volumes:
      - .:/app
      - yarn_cache:/app/node_modules

  postgres:
    image: postgres:9.6
    ports:
      - '5432'
    volumes:
      - postgres_data:/var/lib/postgresql

  redis:
    image: redis:3.2
    ports:
      - '6379'

volumes:
  postgres_data:
  rubygems_cache:
  yarn_cache:
